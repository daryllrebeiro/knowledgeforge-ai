name: CI

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "17 3 * * 1"

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: latest
      - name: Install dependencies
        run: uv sync --dev
      - name: Lint
        run: uv run ruff check .
      - name: Format check
        run: uv run ruff format --check .
      - name: Type check
        run: uv run mypy src
      - name: Unit tests with coverage
        run: >-
          uv run pytest -m "not integration and not live"
          --cov=knowledgeforge --cov-report=term --cov-report=json:coverage.json
      - name: Coverage ratchet
        run: uv run python scripts/coverage_ratchet.py coverage.json

  integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_DB: knowledgeforge
          POSTGRES_USER: knowledgeforge
          POSTGRES_PASSWORD: knowledgeforge
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U knowledgeforge -d knowledgeforge"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    env:
      DATABASE_URL: postgresql://knowledgeforge:knowledgeforge@localhost:5432/knowledgeforge
      REDIS_URL: redis://localhost:6379/0
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: latest
      - name: Install dependencies
        run: uv sync --dev
      - name: Apply migrations
        run: uv run python scripts/apply_migrations.py
      - name: Real PostgreSQL integration tests
        run: uv run pytest -m integration

  emulator-stack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Start local emulator stack
        run: docker compose -f docker-compose.full.yml up -d --build --scale smoke=0
      - name: Run async ingestion smoke test
        run: docker compose -f docker-compose.full.yml run --rm smoke
      - name: Collect emulator logs
        if: always()
        run: docker compose -f docker-compose.full.yml logs --no-color
      - name: Stop local emulator stack
        if: always()
        run: docker compose -f docker-compose.full.yml down --volumes --remove-orphans

  emulator-chaos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Start local emulator stack with chaos overrides
        run: >-
          docker compose -f docker-compose.full.yml -f docker-compose.chaos.yml
          up -d --build --scale smoke=0
      - name: Run chaos drills (Redis loss + Pub/Sub redelivery storm)
        run: docker compose -f docker-compose.full.yml -f docker-compose.chaos.yml run --rm chaos
      - name: Collect emulator logs
        if: always()
        run: >-
          docker compose -f docker-compose.full.yml -f docker-compose.chaos.yml
          logs --no-color
      - name: Stop local emulator stack
        if: always()
        run: >-
          docker compose -f docker-compose.full.yml -f docker-compose.chaos.yml
          down --volumes --remove-orphans

  live-external:
    if: github.event_name == 'schedule' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: latest
      - name: Install dependencies
        run: uv sync --dev
      - name: Gemini live smoke test
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: uv run python scripts/gemini_smoke_test.py
      - name: Live prompt-injection behavior test
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: uv run pytest -m live
      - name: Live local-corpus retrieval evaluation
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: uv run python evaluation/run_live_eval.py
      - name: Phase 12 retrieval evaluation
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: uv run python evaluation/run_phase12_eval.py --output phase12-evaluation.json
      - name: Upload Phase 12 evaluation
        uses: actions/upload-artifact@v4
        with:
          name: phase12-evaluation
          path: phase12-evaluation.json

  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Terraform setup
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.15.9
      - name: Terraform format
        run: terraform fmt -check -recursive infrastructure/terraform
      - name: Terraform init
        working-directory: infrastructure/terraform
        run: terraform init -backend=false
      - name: Terraform validate
        working-directory: infrastructure/terraform
        run: terraform validate
