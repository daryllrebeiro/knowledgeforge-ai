# Operations

The API supports synchronous ingestion for local development and asynchronous ingestion
through a worker for cloud deployments. Request IDs, structured logs, quotas, retries,
and circuit breakers make failures diagnosable.
